<div class="container">
  <section>
    <a id="hero"></a>
    <h1>
      Арена Квантум &mdash; <br /> лучший VR-отдых <br /> в Екатеринбурге
    </h1>
    <p class="subtitle">
      Полное погружение, <br /> топовое оборудование, <br /> приятная атмосфера
    </p>
    <a href="#tariffs"><button class="primary">В новый мир</button></a>
  </section>
</div>

<style lang="scss">
  @use "../../styles/theme";
  @use "../../styles/responsive";

  .container {
    background-image:
      url(/hero.png), radial-gradient(rgba(theme.$accent, 60%), transparent);
    background-position:
      calc(50% + var(--hero-offset)) bottom,
      5% 55%;
    background-size:
      auto 80%,
      150% 125%;
    background-repeat: no-repeat;

    @include responsive.xs() {
      --hero-offset: 64px;
    }

    @include responsive.s() {
      --hero-offset: 288px;
    }

    @include responsive.m() {
      --hero-offset: 288px;
    }

    @include responsive.l() {
      --hero-offset: 384px;
    }
  }

  section {
    margin-top: 0;
    padding-top: 64px;
    padding-bottom: 64px;
  }

  h1 {
    margin: 96px 0 16px 0;
  }

  .subtitle {
    margin: 16px 0;
    color: theme.$secondary-fg;

    @include responsive.xs() {
      font-size: 12px;
    }

    @include responsive.s() {
      font-size: 16px;
    }

    @include responsive.m() {
      font-size: 24px;
    }

    @include responsive.l() {
      font-size: 32px;
    }
  }

  button {
    margin-top: 48px;
  }
</style>
